[project]
name = "deeplearning-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "load-dotenv>=0.1.0",
    "mlflow>=3.7.0",
    "wandb>=0.23.1",
]
